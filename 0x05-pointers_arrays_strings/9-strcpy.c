#include <string.h>
#include "main.h"

/**
 * _strcpy - return a pointer copy of a string
 * @dest: first para
 * @src: second para
 * Return: void
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
